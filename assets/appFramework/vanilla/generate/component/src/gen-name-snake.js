/**
 * Gen Name Human component class.
 *
 * @export
 * @class GenNamePascal
 */
export class GenNamePascal {
    /**
     * Message displayed in the view.
     * @type {string}
     */
    message;

    /**
     * Creates an instance of GenNamePascal.
     */
    constructor() {
        this.message = "Hello Gen Name Human";
    }

    /**
     * Render the component.
     * @returns {void}
     */
    render(parent) {
        const style = document.createElement("style");
        style.type = "text/css";
        style.appendChild(document.createTextNode(".gen-name-snake { font-size: 20px }"));
        parent.appendChild(style);

        const elem = document.createElement("span");
        elem.innerText = this.message;
        elem.className = "gen-name-snake";
        parent.appendChild(elem);
    }
}

/* Generated by UniteJS */
