/**
 * {GEN_NAME_HUMAN} value converter class.
 *
 * @export
 * @class {GEN_NAME_PASCAL}
 */
export class {GEN_NAME_PASCAL}ValueConverter {
    /**
     * Convert the value to a view.
     * @param {any} value
     * @returns {string}
     */
    public toView(value: any): string {
        return value ? value.toString() : "";
    }

    /**
     * Convert the value from a view.
     * @param {string} view
     * @returns {any}
     */
    public fromView(view: string): any {
    }
}
