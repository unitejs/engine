/**
 * Gen Name Human binding behavior class.
 *
 * @export
 * @class GenNamePascalBindingBehavior
 */
import { Binding, Scope } from "aurelia-binding";

export class GenNamePascalBindingBehavior {
    /**
     * Connects the binding to the scope.
     * @param {Binding} binding
     * @param {Scope} scope
     * @returns {void}
     */
    public bind(binding: Binding, scope: Scope): void {
    }

    /**
     * Disconnects the binding from a scope.
     * @returns {void}
     */
    public unbind(): void {
    }
}
