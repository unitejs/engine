/**
 * {GEN_NAME_HUMAN} binding behavior class.
 *
 * @export
 * @class {GEN_NAME_PASCAL}
 */
export class {GEN_NAME_PASCAL}BindingBehavior {
    /**
     * Connects the binding to the scope.
     * @param {Binding} binding
     * @param {Scope} scope
     * @returns {void}
     */
    bind(binding, scope) {
    }

    /**
     * Disconnects the binding from a scope.
     * @returns {void}
     */
    unbind(binding, source) {
    }
}
