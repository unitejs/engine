/**
 * {GEN_NAME_HUMAN} Binding Behavior class.
 */
import { Binding, Scope } from "aurelia-binding";

export class {GEN_NAME_PASCAL}BindingBehavior {
    public bind(binding: Binding, source: Scope): void {
    }

    public unbind(binding: Binding, source: Scope): void {
    }
}
