/**
 * {GEN_NAME_HUMAN} element class.
 *
 * @export
 * @class {GEN_NAME_PASCAL}
 */
import { bindable } from "aurelia-templating";

export class {GEN_NAME_PASCAL} {
    /**
     * Bindable value for the element.
     * @type {string}
     */
    @bindable
    public value: string;

    /**
     * Element value has changed.
     * @param {any} newValue
     * @param {any} oldValue
     * @returns {void}
     */
    public valueChanged(newValue: string, oldValue: string): void {
    }
}
