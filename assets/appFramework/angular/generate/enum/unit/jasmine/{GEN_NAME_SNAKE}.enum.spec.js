/**
 * Tests for {GEN_NAME_PASCAL} enum.
 */
import { {GEN_NAME_PASCAL}Enum } from "{GEN_TEST_ROOT}src/{GEN_SUB_FOLDER}{GEN_NAME_SNAKE}.enum";

describe("{GEN_NAME_PASCAL}Enum", () => {
    it("can be created", () => {
        expect({GEN_NAME_PASCAL}Enum).toBeDefined();
    });
});
