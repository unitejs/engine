/**
 * {GEN_NAME_HUMAN} pipe class.
 */
import { Pipe } from "@angular/core";

@Pipe({
    name: "{GEN_NAME_CAMEL}"
})
export class {GEN_NAME_PASCAL}Pipe {
    transform(value, args) {
        return null;
    }
}
