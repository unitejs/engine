/**
 * {GEN_NAME_HUMAN} pipe class.
 */
import { Pipe, PipeTransform } from "@angular/core";

@Pipe({
    name: "{GEN_NAME_CAMEL}"
})
export class {GEN_NAME_PASCAL}Pipe implements PipeTransform {
    public transform(value: any, args?: any): any {
        return null;
    }
}
