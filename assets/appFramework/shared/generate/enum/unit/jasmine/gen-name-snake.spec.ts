/**
 * Tests for GenNamePascal enum.
 */
import { GenNamePascal } from "../../src/gen-name-snake";

describe("GenNamePascal", () => {
    it("can be created", () => {
        expect(GenNamePascal).toBeDefined();
    });
});
