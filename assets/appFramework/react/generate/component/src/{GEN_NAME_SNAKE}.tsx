/**
 * {GEN_NAME_HUMAN} component class.
 */
import * as React from "react";

const {GEN_NAME_CAMEL}Style = {
    fontSize: "20px"
};

export class {GEN_NAME_PASCAL} extends React.Component {
    public render(): JSX.Element {
        return (<span style={{GEN_NAME_CAMEL}Style}>Hello {GEN_NAME_HUMAN}</span>);
    }
}
