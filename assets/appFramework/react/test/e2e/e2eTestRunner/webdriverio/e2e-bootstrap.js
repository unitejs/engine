exports = module.exports = () => {
    browser.timeouts("script", 30000);

    browser.addCommand("waitForReactPage", (timeout) => {
        return browser
            .waitUntil(() => {
                return browser.getText("#root")
                    .then((text) => {
                        return text && text.length > 0;
                    });
            }, timeout ? timeout : 5000);
    });

    browser.addCommand("loadAndWaitForReactPage", (url, timeout) => {
        return browser
            .url(url)
            .waitForReactPage(timeout);
    });
};

/* Generated by UniteJS */
