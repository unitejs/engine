const path = require("path");
const os = require("os");

const parentFolderCount = os.platform === "darwin" ? 7 : 5;

const main = require(path.join(
    __dirname,
    "../".repeat(parentFolderCount),
    "www/build/assets/platform/electron/",
    "main"
));

main.setOptions({
    "showDevTools": true,
    "startUrl": path.join(__dirname, "../".repeat(parentFolderCount), "www", "index.html")
});

// Generated by UniteJS
